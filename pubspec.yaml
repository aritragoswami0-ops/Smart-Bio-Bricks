name: smart_bio_bricks_app
description: Smart Bio Bricks — analytical version
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^3.0.1
  fl_chart: ^0.55.2
  provider: ^6.0.5
  # http not required unless you will fetch remote data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/sample_data.json
